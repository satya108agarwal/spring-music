---
applications:
- name: spring-music-<EmpoyeeId> # Replace <EmpoyeeId> this unique id (Employee ID)
  memory: 1G
  random-route: true
  instances: 1
  path: build/libs/spring-music-1.0.jar
  env:
    JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 17.+ } }'
